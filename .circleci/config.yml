version: 2.1

jobs:
  build:
    # shell: ./secrethub run -- /bin/bash
    docker:
      - image: circleci/golang:latest
        environment:
          TEST: secrethub://marton/test1/password
    steps:
      - checkout
      - run: ./test.sh
