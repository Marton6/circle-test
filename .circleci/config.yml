version: 2.1

orbs:
  secrethub: marton/test-s@dev:alpha

jobs:
  build:
    docker:
      - image: circleci/node:latest
    steps:
      - run: secrethub ls marton/test1
      - secrethub/secrethub-run:
          command: node test.js
